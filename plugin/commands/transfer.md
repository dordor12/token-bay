---
description: Move credits from your home region to another region (federation cross-region transfer)
argument-hint: --to <region> --amount <credits> [--dry-run]
---

!`${CLAUDE_PLUGIN_ROOT}/bin/token-bay-sidecar transfer --config ${HOME}/.token-bay/config.yaml $ARGUMENTS`
