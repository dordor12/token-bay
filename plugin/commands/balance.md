---
description: Show your current Token-Bay credit balance (cached or freshly fetched from the tracker)
---

!`${CLAUDE_PLUGIN_ROOT}/bin/token-bay-sidecar balance --config ${HOME}/.token-bay/config.yaml`
