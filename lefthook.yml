pre-commit:
  parallel: true
  commands:
    gofumpt:
      glob: '*.go'
      run: gofumpt -l -w {staged_files}
      stage_fixed: true
    vet:
      glob: '*.go'
      run: go vet ./...
    golangci-lint:
      glob: '*.go'
      run: golangci-lint run --new-from-rev=HEAD
